import React from "react";
import ReactDOM from "react-dom/client";
// ganti disini
import App from "./App-fetch.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
